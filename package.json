{
  "name": "powergrid",
  "version": "1.0.0",
  "description": "PowerGrid: Advanced Electricity Management & Billing System (MERN)",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix client",
    "seed": "npm run seed --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
